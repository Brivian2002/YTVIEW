[build]
  command = "npm run build"
  publish = "dist"   # Vite outputs to "dist", not ".next"

# Remove the Next.js plugin – it's not needed and will cause errors
# [[plugins]]
#   package = "@netlify/plugin-nextjs"
